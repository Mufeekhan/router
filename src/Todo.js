import React from 'react';
const Todo = ({todos}) => {
    const todoList =todos.length ? (
        todo.map(todoList)
    ): ()
    <p className='center'>you have no todo's left,yaya1</p>
    return (  
        <div className="todos collection"></div>
    );
}
 
export default Todo;